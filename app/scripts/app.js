(function (angular) {
    'use strict';
    var dependencies = [
        'ngRoute',
        'theme.templates'
    ];
    // define our app as an angular module
    return angular.module("app", dependencies);
}(angular));
